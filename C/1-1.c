/*

The "hello" word will be save in str variable.
but the the character '\0' can not be saved into str[5].

*/



#include <stdio.h>

int main()
{

    // Assume: 
    char str[ 6 ];
    scanf( "%s", str ); /* User types hello */
    
}
